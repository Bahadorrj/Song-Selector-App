# Music Selector

A web application showcasing a collection of songs (in static/songs) with interactive selection functionality. Visitors have playback features. Additionally, they can select their favorite piece and submit their choice along with a name.

### There are 2 versions of the project. Visit the related branch based on your preference:
1. ***sqlite***
2. ***postgreSQL***

## Technologies
- **HTML5**
- **CSS3**
- **JavaScript**
- **Flask** (Python)

## Setup
1. Clone the repository
2. Install dependencies:
```bash
pip install -r requirements.txt
```
3. Run the application locally:
```bash
python app.py
```
4. Visit `http://localhost:5000` in your browser